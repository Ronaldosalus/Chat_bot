# Marco!

Eu não consegui colocar ele em .exe, então, vai precisar das bibliotecas.
( obs: precisa ter python com pip, então pesquisa: https://www.youtube.com/watch?v=MvUMAvmviaE )
comandos no cmd:

- pip install customtkinter
- pip install langchain-groq
- pip install gtts
- pip install playsound
- pip install pypdf2
- pip install tkinter

As vezes, pode dar um erro no tkinter e no langchain, o do langchain precisa instalar os arquivos build do
VS Code de que involva C++, e o tkinter é só criar um novo diretório.
Todos os arquivos estão salvos tanto no git quanto github, além de ter o Z3K aclopado ai dentro também.

